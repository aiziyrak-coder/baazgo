# Server (server.ts / API_ONLY)
PORT=3000
NODE_ENV=development
TRUST_PROXY=0
API_ONLY=false
CORS_ORIGINS=https://baazgo.ziyrak.org,http://localhost:5173
RATE_LIMIT_MAX_PER_MIN=200

# Frontend build (Vite — prefix VITE_)
# Empty = same origin as the page (dev with tsx server)
VITE_API_BASE_URL=
